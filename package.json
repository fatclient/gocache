{
  "name": "gocache",
  "version": "0.1.0",
  "description": "Structure keeps an objects and either returns it or calls a callback to refresh it, if expired.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cache"
  ],
  "author": "fatclient <fatclient@protonmail.com>",
  "license": "ISC",
  "dependencies": {}
}
